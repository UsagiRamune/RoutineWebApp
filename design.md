# Design reference — RoutineWebApp

สถานะ: ทิศทางยืนยันแล้ว (สังเคราะห์จาก Stitch 3 ทิศทาง) — เริ่ม rollout ทีละหน้า เริ่มจาก Dashboard

อ่านไฟล์นี้ก่อนแก้ UI ทุกครั้ง ถ้าพรอมป์ไหนขัดกับที่นี่ ให้ถามก่อนแก้แทนการเดา

## หลักการ (สำคัญสุด — อ่านก่อนอย่างอื่น)

**ไม่ใช่ทุกอย่างสำคัญเท่ากัน อย่าห่อทุกอย่างด้วย card แบบเดียวกันหมด**

แบ่งเนื้อหาเป็น 3 ระดับก่อนออกแบบทุกหน้า:
1. **Hero content** — ตัวเลข/ข้อมูลสำคัญที่สุดในหน้านั้น ไม่ต้องมีกรอบ ใช้ขนาด/น้ำหนักตัวอักษรทำหน้าที่แทนกรอบ
2. **Secondary card** — ข้อมูลรองที่ "เทียบกันได้เป็นกลุ่ม" (เช่น น้ำหนัก+ก้าว) จัดใน panel เดียวกัน มีเส้นแบ่งบางๆ คั่นระหว่างรายการ ไม่ใช่แยกเป็นกล่องเดี่ยวๆ
3. **List row** — เนื้อหาที่เป็น**ลิสต์โดยธรรมชาติ** (agenda วันนี้, routine checklist, calendar, log ประวัติ) → ลอยไม่มีกรอบเต็ม ใช้เส้นสีบางด้านซ้ายบอกหมวดหมู่แทน

**ข้อยกเว้น — ยังใช้กรอบ/panel เต็มได้ตามปกติ:** ฟอร์มกรอกข้อมูล, การ์ดรายละเอียด (project detail, settings), modal — สิ่งเหล่านี้คือ "วัตถุมีขอบเขต" ไม่ใช่ลิสต์ ไม่ต้องแปลงเป็น list row

**กฎค่าจริง vs เป้า (ใช้ทุก metric ในแอป ไม่ใช่แค่ dashboard):**
- ตัวเลข**ที่ทำได้จริง**วันนี้ (ก้าว, kcal ที่กิน, น้ำหนักปัจจุบัน) ต้องใหญ่/หนักกว่าตัวเลข**เป้า**เสมอ
- **ห้ามโชว์ % หรือ "เหลืออีก/remaining" แบบเด่น** — ถ้าจะให้ดูก็เล็กจางไว้ท้ายสุด ไม่ใช่ประเด็นหลักของสายตา
- Trend/delta (เช่น น้ำหนัก ▼0.3) ให้ขนาดกลาง — เห็นชัดพอจับสังเกตได้ แต่ไม่แย่งซีนตัวเลขหลัก

ก่อนส่งงาน ถามตัวเอง: "ถ้าลบกรอบออกจากบล็อกนี้ ยังรู้ได้ไหมว่ามันสำคัญแค่ไหน" ถ้าตอบไม่ได้ แปลว่ากำลังใช้ card แบบขี้เกียจ

## สี

พื้นหลัง: `#14171F` (หน้า) / `#1B1F2A` (panel) / `#2A2F3D` (เส้นขอบ)
ตัวอักษร: `#EDEAE0` (หลัก) / `#7C8394` (รอง/hint)

**สีหมวดหมู่ — คงที่ตามของเดิมในแอป ห้ามเปลี่ยนเป็น hex ของ Stitch:**
- ร่างกาย/สุขภาพ: `#F0A345` (ส้ม)
- ปฏิทิน/งาน: `#4FC1E0` (ฟ้า)
- โปรเจกต์/สกิล: `#9B7EDE` (ม่วง)
- อันตราย/ลบ/เกินเป้า (เชิงลบ): `#E4574A`
- บวก/แนวโน้มดี (เช่นน้ำหนักลด): เขียวโทนเดียวกับที่ใช้ใน history อยู่แล้ว (เช็คในโค้ดจริง ไม่ต้องเดาใหม่)

ใช้สีหมวดหมู่เป็น **เส้นบอกหมวด** (list row) หรือ **accent เล็กๆ** ก่อนเสมอ ไม่ใช่พื้นหลังเต็มบล็อก

## Typography

**Font 2 ตระกูล เท่านั้น:**
- **Geist** — ข้อความทั่วไป, ชื่อรายการ, คำอธิบาย, ปุ่ม
- **Geist Mono** — ตัวเลขและเวลาเท่านั้น (ยอดเงิน/แคล/น้ำหนัก/ก้าว/timestamp/countdown) — ไม่ใช้กับ label ข้อความทั่วไป

ติดตั้งผ่าน `next/font/google` (ทั้งสองตัวมีบน Google Fonts อยู่แล้ว)

**Label เล็ก (eyebrow/หัวข้อย่อย):**
- ใช้ ALL CAPS ได้ แต่ต้อง: ตัวเล็ก (~11px), น้ำหนักเบา-กลาง, letter-spacing กว้างเล็กน้อย (~0.05em), สีจาง (`#7C8394` หรือจางกว่า)
- ใช้เฉพาะ label ที่สำคัญจริง (หัวข้อ section, หมวดหมู่) — **ไม่ใช่ทุก label ในหน้า**

**ห้าม:** ตัวเอียง (italic) เป็น decorative element, font serif ใดๆ

## Layout patterns

**Progress bar → ใช้แบบแบ่งช่อง (segmented), ไม่ใช่แท่งเรียบยาว**
- แบ่งเป็นช่องสั้นๆ เรียงกัน (8-10 ช่องตามความเหมาะสม) แทนแท่งเดียวยาว
- ช่องที่ผ่านแล้วติดสี ช่องที่เหลือจาง/เทา
- ใช้แทน progress bar เดิมทุกจุดในแอป (calorie, water, protein, steps, ฯลฯ)

**Metric ที่จัดกลุ่มได้ (เช่น น้ำหนัก+ก้าว)** — ใส่ใน panel เดียวกัน มีเส้นแบ่งแนวตั้งบางๆ คั่น ไม่ใช่แยกเป็น 2 การ์ด

## หน้าที่ทำตาม pattern นี้แล้ว

- [x] Dashboard (`app/page.tsx`) — redesign ครบตาม design.md แล้ว

## หน้าที่ยังเป็นของเดิม (card แบบเดียวกันหมดทุกบล็อก)

- Routine, History, Nutrition, ร่างกาย, Calendar, Projects, Workout — รอ rollout ทีละหน้า